# Copy to .env.local for real backend development, or set VITE_MOCK=true to
# run the whole frontend against the in-memory fake API/WS (no backend needed).
VITE_API_URL=http://localhost:8000
VITE_WS_URL=ws://localhost:8000
VITE_MOCK=false
